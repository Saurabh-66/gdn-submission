[solution]
name = "ELOTracker-solution-v1"      # Solution name
definition = "gated_delta_net"          # Track: fused_moe | sparse_attention | gated_delta_net
author = "ELOTracker"              # Team/author name

[build]
language = "triton"               # triton | cuda
entry_point = "kernel"            # Kernel function name
