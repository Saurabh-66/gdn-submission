[solution]
name = "ELOTracker-solution-v2"      # Solution name
definition = "gdn_decode_qk4_v8_d128_k_last"          # Track: fused_moe | sparse_attention | gated_delta_net
author = "ELOTracker"              # Team/author name

[build]
language = "triton"               # triton | cuda
entry_point = "kernel.py::kernel"            # Kernel function name
